services:
  estimate-microservice:
    build:
      context: .
      dockerfile: src/GtMotive.Estimate.Microservice.Host/Dockerfile
    container_name: estimate-microservice
    ports:
      - "5080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Development
